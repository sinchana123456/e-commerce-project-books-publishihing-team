import Button from '../UI/Button';
import classes from './Home.module.css';

const Home = () => {

    

    return(
        <section className={classes.home}>
            <h2>Books</h2>
        </section>
    );
};

export default Home;